# Prominent section navigation

Connor requested that the subtle tabs over the header photo move below it onto white, span the screen, and become larger, bold and easy to discover.

The five landing pages now render a full-width white navigation strip immediately after the shared photo hero. Five equal desktop columns use bold navy labels and gold Lucide icons. The active section has a navy fill, white type and gold underline. Mobile uses two columns with the fifth link spanning the row so all destinations remain visible. Keyboard focus outlines and aria-current remain available. Compact navigation in article detail headers remains unchanged.

TypeScript, targeted ESLint and git diff whitespace checks passed. Safari desktop preview verified the placement, contrast, labels and selected state on Featured Brands. Deployment uses the required guarded npm run deploy command.

Deployed Cloudflare version: 823f27e6-39d7-4777-9482-b8edf8729bc7. Live checks passed for all five routes: one prominent navigation after the hero, five links, and one active selection. Production login and portal smoke checks passed. Safari confirmed the published Newsletter navigation on white below the photo.
